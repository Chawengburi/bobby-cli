# .specs-local.md — template

**นี่คือไฟล์ตัวอย่าง** คัดลอกเป็น `.specs-local.md` แล้วเติมค่าจริง
(`cp .specs-local.example.md .specs-local.md`)

`.specs-local.md` **อยู่ใน `.gitignore` และต้องไม่ถูก commit** — repo นี้เป็น
**public** ส่วนค่าข้างล่างเป็นชื่อภายในของระบบ

## ทำไมต้องมีไฟล์นี้

เอกสารใน `specs/` เขียนด้วย placeholder แทนชื่อจริง เพื่อให้ spec เผยแพร่ได้โดย
ไม่พาชื่อโดเมนภายใน, ชื่อ account, และ id ของคนออกไปด้วย — แต่ agent ที่ทำงานจริง
ต้องรู้ค่าเหล่านั้นถึงจะยิง request ได้ **agent อ่านไฟล์นี้เพื่อแทนค่า**

> ค่าที่ **ไม่** อยู่ในไฟล์นี้เพราะเป็นความลับจริง ๆ ไม่ใช่แค่ชื่อภายใน:
> uploader token (Token A) อยู่ที่ `~/.uploader-token` (chmod 600, นอกทุก repo)
> · `sm_live_*` ของผู้ใช้อยู่ที่ `~/.bobby-cli/credentials.json`
> **ห้ามเอาค่าเหล่านี้มาไว้ในไฟล์นี้ไม่ว่ากรณีใด**

## ตารางแทนค่า

| Placeholder | ค่าจริง | ใช้ที่ไหน |
|---|---|---|
| `{{UPLOADER_HOST}}` | `uploader.example.com` | spec 17 § 0/§ 2, spec 18 § 5.1 — host ของ uploader (ต้องเป็น `https://`) |
| `{{DEV_CF_ACCOUNT}}` | `your-cf-account` | spec 17 § 4.1 — subdomain ของ Cloudflare account ฝั่ง dev/test ที่ `bobby-cli auth show --json` ต้องแสดง |
| `{{OWNER_DISCORD_ID}}` | `000000000000000000` | spec 18 § 7 — Discord user id ของ owner ใน `commands.ownerAllowFrom` |

## หมายเหตุ

- ชื่อ account ฝั่ง production ถูก bake ไว้ใน `src/core/config.ts` อยู่แล้วโดยเจตนา
  (เหตุผลอยู่ใน `CLAUDE.md` ของ workspace) จึงไม่ต้องมี placeholder ให้
- ถ้าเพิ่ม placeholder ใหม่ในสเปก **ต้องเพิ่มแถวในไฟล์นี้และในตัวอย่างนี้พร้อมกัน**
  ไม่งั้นคนต่อไปจะเจอ placeholder ที่ไม่มีใครรู้ว่าแทนอะไร
